export const dollarUS = Intl.NumberFormat("en-US", {
    style: "currency",
    currency: "USD",
});

export const realBR = Intl.NumberFormat("pt-BR", {
    style: "currency",
    currency: "BRL",
});